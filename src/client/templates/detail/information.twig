<div class="card border-0 bg-transparent">
    <h3 class="h4 card-header text-dark py-2 bg-white">
        {% if data.isContract %}Contract{% else %}Address{% endif %} Information
    </h3>
    <table class="table table-borderless table-striped m-0 table-dark table-sm bg-transparent">
        <tbody>
            {% if data.address %}
            <tr>
                <th scope="row">{% if data.isContract %}Contract{% else %}Address{% endif %}</th>
                <td class="list-field">{{ data.address|etherscan(data, 'no-contract') }}</td>
            </tr>
            {% endif %}
            {% if data.token.createdAt %}
            <tr>
                <th scope="row" class="text-nowrap">Created At</th>
                <td class="list-field">{{ data.token.createdAt|localdate }}</td>
            </tr>
            {% endif %}
            {% if data.isContract and data.contract and data.contract.creator %}
            <tr>
                <th scope="row">Creator</th>
                <td class="list-field">{{ data.contract.creator|ethplorer(data, 'no-contract') }}</td>
            </tr>
            {% endif %}
            {% if data.token.createdTx %}
            <tr>
                <th scope="row" class="text-nowrap">Create Tx</th>
                <td class="list-field">{{ data.token.createdTx|ethplorer(data) }}</td>
            </tr>
            {% endif %}
            <tr>
                <th scope="row">Balance</th>
                <td class="list-field">{{ data.balance|ether-full(data) }}</td>
            </tr>
            {% if data.balanceIn %}
            <tr>
                <th scope="row" class="text-nowrap">Total In</th>
                <td class="list-field">{{ data.balanceIn|ether }}</td>
            </tr>
            {% endif %}
            {% if not data.hideBalanceOut and data.balanceOut %}
            <tr>
                <th scope="row" class="text-nowrap">Total Out</th>
                <td class="list-field">{{ data.balanceOut|ether }}</td>
            </tr>
            {% endif %}
            {% if data.token.txsCount %}
            <tr>
                <th scope="row">Transactions</th>
                <td class="list-field">{{ data.token.txsCount|int }}</td>
            </tr>
            {% endif %}
            {% if data.isContract and data.contract.isChainy and data.contract.txsCount %}
            <tr>
                <th scope="row">Transactions</th>
                <td class="list-field">{{ data.contract.txsCount|int }}</td>
            </tr>
            {% endif %}
        </tbody>
    </table>
</div>
